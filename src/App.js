import React, { Suspense, lazy } from 'react';
import './App.css';
import { Switch, Route } from 'react-router-dom';



function App() {
  return (
    <Suspense fallback={}>
      <Switch>
        
      </Switch>
    </Suspense>
  );
}

export default App;
